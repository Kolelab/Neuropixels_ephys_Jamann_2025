function [Evoked,Delays,Spiking] = extract_opto_spikes(vec_StimTimes,vec_SpikeTimes,vec_StimEnd,addition)
%UNTITLED2 Extract opto evoked spikes from stim and spiketimes
% 
%Evoked is a cell array with absolute spike times per trial;
%Delays is a cell arraw with relative to onset times
%Spiking is a cell array to indicate if there is a spike (1) or not  (O)





end